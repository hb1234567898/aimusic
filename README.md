# ORBIT Music

基于 React + Vite 的本地音乐播放器，包含 19 首 VALORANT 与英雄联盟歌曲、对应封面和同步 LRC 歌词。

## 开发

```bash
npm install
npm run dev
```

访问 <http://127.0.0.1:8765/>。

## 构建

```bash
npm run build
```

构建产物位于 `dist/`。音频、歌词和封面保存在 `public/assets/`。
